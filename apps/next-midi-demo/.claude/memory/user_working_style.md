---
name: user_working_style
description: "How the user wants to work — avoidant, low-resource, needs tight pairing and shrunk scope"
metadata:
  node_type: memory
  type: user
  originSessionId: 1a917f98-450a-4fee-9d56-6708b2856d56
---

The user has been avoiding the next-midi-demo project for ~half a year; finishing it is emotionally heavy and a source of real distress. They describe themselves as "very not resourceful" (low energy/executive function).

**How to apply:**
- Carry the executive load. Propose the next *small, concrete, reversible* step rather than handing them open-ended choices. Shrink the mountain to one rock.
- Pair tightly: discuss before writing larger changes, but keep momentum — landing a concrete, typechecking win beats more deliberation.
- Externalize scope as a finite task list so the work stops feeling infinite.
- Name legitimate scoping/deferral (e.g. walling off slow strums) as engineering, not failure — but do it *honestly* (loud `dieMessage`), never silent landmines.
- Warm and direct; validate the fear, then move. Avoid long walls of text when they're low.
- Their avoidance trap is "refactoring that doesn't bring release closer" — frame foundational work explicitly in terms of how it unblocks the release. See [[project_next_midi_demo_scheduling]].
