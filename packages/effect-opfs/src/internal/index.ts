console.log()

// Note: In earlier versions of the spec, close(), flush(), getSize(), and
// truncate() were wrongly specified as asynchronous methods, and older versions
// of some browsers implement them in this way. However, all current browsers
// that support these methods implement them as synchronous methods

// TODO: when I implement them, I need to ensure I handle them polymorphically
