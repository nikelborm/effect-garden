// TODO: when targeting single-file schemas (automatically packaging about 200
// vs code extensions or more) a cool strategy for rebuilding the extension
// would be running it against git diff between commits. we can make an
// extension build on commit. remember hash, and when the time passes, we can
// pull the updates, see the relative diff since last release, and only
// rerelease the neccessary packages
