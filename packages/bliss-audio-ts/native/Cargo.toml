[package]
name = "bliss-native"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]
name = "bliss_native"

[dependencies]
bliss-audio = { version = "0.11.2", default-features = true }
ndarray = "0.16.1"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
